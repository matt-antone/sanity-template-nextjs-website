import * as React from 'react';

interface ILayouPageProps {
}

const LayouPage: React.FunctionComponent<ILayouPageProps> = (props) => {
  return (
    <div>Page</div>
  );
};

export default LayouPage;
